{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop window permissions.",
  "windows": ["main"],
  "permissions": ["core:default", "process:default", "shell:default", "dialog:open"]
}
