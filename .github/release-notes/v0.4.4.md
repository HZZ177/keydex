本次更新重点提升了长会话连续性与文件阅读体验。

## 长会话与上下文恢复

- 优化上下文压缩机制，按真实对话轮次保留用户请求、Agent 操作、执行结果和当前状态。
- 提升连续多次压缩后的上下文恢复可靠性，减少引用、评论、Skill、附件等内容重复、丢失或顺序错乱。
- 保留最近读取的文件记录，并在恢复上下文时根据文件当前内容重新加载。
- 优化近期执行现场的保留策略，让长任务经过上下文压缩后能够更稳定地继续执行。
- 支持直接打开对话中引用的本地绝对路径文件。

## Markdown 阅读体验

- 在 Markdown 预览与源码模式之间切换时保持当前阅读位置，避免跳回顶部。
